apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: hpa-vpi-app 
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vpi-app
  minReplicas: {{ .Values.minReplicas }}
  maxReplicas: {{ .Values.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: {{ .Values.targetUtilizationType }}
      target:
        type: Utilization
        averageUtilization: {{ .Values.targetAverageUtilization }}

